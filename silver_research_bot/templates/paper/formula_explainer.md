对论文中的每个数学公式逐条进行系统、全面的解读。直接输出完整的 HTML 片段，禁止加入问候语、角色介绍、思考过程或元评论，禁止使用 Markdown 代码围栏包裹。

## 解读层次（每个公式必须涵盖）

1. **符号定义**: 逐一说明公式中每个符号/变量在论文背景下的具体含义（如 p_i 表示第 i 个用户的发射功率，d_{mi} 表示用户 m 与服务器 i 之间的距离）
2. **数学含义**: 说明该公式在数学上表达了什么关系（如加权求和、条件约束、优化目标函数等）
3. **物理/领域含义**: 在论文研究领域（通信、机器学习、优化等）中，该公式代表什么实际意义
4. **关联关系**: 该公式使用了哪些已定义量的组合，与论文中其他公式有何推导或依赖关系

## 输出格式

每个公式输出为一个 `.frow` 卡片，按分类分组。严格按照以下 HTML 结构：

```html
<p class="sec-title">分类名称</p>

<div class="frow">
<div class="fnum">式 N</div>
<div class="fbody">
<span class="ftag tag-xxx">分类标签</span>
<div class="fexpr">公式的 LaTeX 表达式（原文符号）</div>
<div class="fmean">公式含义的系统解读，<b>关键术语</b>用 b 标签加粗，涵盖符号定义、数学含义和领域含义</div>
</div>
</div>
```

## 分类标签的 CSS class 对照

- 系统状态描述 / 系统模型 / 通信模型 → tag-sys
- 问题建模 / MDP定义 / MDP状态 → tag-mdp
- 奖励函数 / 奖励 → tag-rwd
- 算法核心 / 策略梯度 / 优化 → tag-alg
- GAT编码器 / 图注意力 / 神经网络 → tag-gat
- 观测优化 / 状态处理 → tag-obs
- 动力学 / 能耗模型 → tag-sys
- 其他无法归类的公式 → tag-sys

## 要求

1. 识别每个公式所属的类别，按类别分组，每组前加 `<p class="sec-title">类别名称</p>`
2. 同一类别内的公式按编号顺序排列
3. 公式 LaTeX 表达式放在 `<div class="fexpr">` 中。**fexpr 中的 LaTeX 表达式必须与下方用户消息中提供的公式原文逐字相同，禁止截断、改写、省略或增删任何符号。**若公式有编号，在 `.fnum` 中标注"式 N (编号X)"。不要用 $ 或 $$ 包裹
4. 含义解读放在 `<div class="fmean">` 中，需为每个公式覆盖符号定义、数学含义和领域含义三个层面，关键概念用 `<b>` 加粗。**解读文字中出现的所有 LaTeX 数学符号、变量名、表达式必须用单个 `$` 包裹**（如 `$v_i^R(t)$`、`$\mathbf{Q}_i^R(t)$`、`$\Delta t$`、`$\|\cdot\|$`），以便前端 MathJax 渲染为数学符号。注意 `$` 要紧贴数学表达式，不要与中文之间留空格
5. 公式编号用中文"式 N"格式，若提供公式编号（如(5)），在类名后标注
6. 每个公式必须解释，不跳过，不合并
7. 直接输出 HTML，禁止用 ```html 包裹，禁止加任何前言后语
8. **关键**: 只解释我提供的完整公式。禁止自行创建碎片公式卡片（如单个变量、运算符片段、中间步骤片段）。若提供的公式 LaTeX 表达式本身过短（少于5个字符且无 LaTeX 命令），请不要为其生成卡片，直接跳过。
