深入分析论文的**系统模型**。直接输出分析内容，禁止包含问候语、角色介绍、思考过程或元评论。

## 输出格式要求（严格遵守）

1. **所有数学符号必须用 $...$ 包裹**：集合 $\mathcal{I}$、$\mathcal{M}$，变量 $a_i$、$H_i^m$，参数 $\mu_i^t$、$\mu_i^e$
2. **所有公式必须保留原文编号**：如 $h_{i,m} = \beta_0 d_{i,m}^{-\alpha} \tilde{h}$ (3)，$$C_{i,m} = B \log_2(1 + \gamma_{i,m})$$ (7)
3. **信道模型必须给出完整 LaTeX 表达式**：包括路径损耗、小尺度衰落（莱斯/瑞利）、LoS 概率公式、SINR 表达式
4. **计算模型必须给出完整公式**：本地计算时延 $T_i^L = a_i D / f_i^L$ (5)、能耗 $E_i^L = \kappa (f_i^L)^2 a_i D$ (6)、卸载时延 $T_{i,m}^O = a_i D / R_{i,m}$ (8) 等
5. **禁止仅用文字描述数学表达式**，禁止写"信道增益由公式给出"而不给出公式本身

## 分析维度

### 1. 系统架构
- 系统由哪些实体组成（传感器、边缘服务器、云、用户设备、无人机等）
- 各组件间的交互关系（数据流、控制流、通信链路）
- 用文字描述系统逻辑架构的层次关系和接口

### 2. 关键假设与约束
- 核心假设（信道模型、计算能力、同步/异步假设等）
- **完整写出信道模型的 LaTeX 公式**（路径损耗 + 小尺度衰落 + LoS 概率）
- 物理约束和资源限制（能量预算、带宽、时延、计算上限），给出精确数学表达式
- 假设的合理性评估

### 3. 数学模型表达
- 各关键量的数学描述（集合、变量、参数、函数），逐项给出 LaTeX 定义
- 状态变量、决策变量、参数的定义和完备性
- 模型可扩展性

### 4. 模型评价
- 与相关工作的创新点对比
- 复杂度与表达力的平衡
- 潜在限制或不足

对较难部分详细拆解，给出直观物理解释和数学推导。

### 5. 结构化总结
用简洁的要点列表总结系统模型的核心要素：实体组成、关键假设（含公式编号）、数学模型特征、创新点。
