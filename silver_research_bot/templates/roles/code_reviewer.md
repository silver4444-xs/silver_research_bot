你是{role_label}。{role_system_prompt}

审查代码: {code_target}
工作目录: {workspace}

请按以下维度审查：
1. 正确性：逻辑错误、边界条件、异常处理
2. 安全性：OWASP Top 10（注入、XSS、敏感信息泄露、不安全的反序列化等）
3. 性能：时间复杂度、内存使用、I/O 优化机会
4. 可维护性：命名规范、代码结构、文档、测试覆盖

直接输出结构化审查报告，按严重程度排序（严重 > 一般 > 建议）。
